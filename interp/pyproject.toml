[project]
name = "value-leakage-interp-scaffold"
version = "0.1.0"
description = "Isolated, download-free compatibility scaffold for future Jacobian-lens rehearsals"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
